@using Codesanook.EventManagement.Models;
@model BankAccountViewModel
@{
    Layout.Title = T("Add bank account").ToString();
}

@using (Html.BeginFormAntiForgeryPost())
{
    @Html.Partial("_BankAccountForm", Model);

    <fieldset>
        <button class="primaryAction" type="submit">Create</button>
    </fieldset>
}
